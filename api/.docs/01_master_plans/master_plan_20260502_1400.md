# Master Plan - Task 1.2b & 1.3: Gin Migration & Dockerization

- Request ID: 20260502_1400
- Date: 2026-05-02 14:00
- User Request: Switch Go HTTP layer from stdlib to Gin and create api/Dockerfile
- Status: Completed

## Request Analysis

**What User Wants:**
1.  Migrate the existing Go API skeleton from `net/http` (stdlib) to `github.com/gin-gonic/gin`.
2.  Create a multi-stage `Dockerfile` for the Go API.
3.  Ensure the project builds successfully with `go build ./...`.
4.  Ensure the Docker image builds successfully with `docker build`.

**Why:**
- Gin provides a more robust and developer-friendly framework for building APIs in Go.
- Dockerization is essential for consistent deployment and containerization.

**Scope:**
- Modify `go.mod`, `internal/http/router.go`, `internal/http/health.go`.
- Remove `internal/http/json.go` if it's just a JSON helper.
- Update `cmd/server/main.go` to use the Gin router.
- Create `Dockerfile` in the `api/` directory.

**Success Criteria:**
- `go build ./...` passes in the `api` directory.
- `docker build -t trade-buddy-api /Users/nick/2_SideProjects/trade-buddy/api` succeeds.
- Health check endpoint `/health` returns expected JSON.

## Task Breakdown

### Task 001: Gin Migration (T1.2b)
- **Type:** Refactor
- **Priority:** Critical
- **What:** Install Gin, update router and health handlers, remove redundant JSON helper.
- **Acceptance Criteria:**
  - `go.mod` includes `github.com/gin-gonic/gin`.
  - `router.go` returns `*gin.Engine`.
  - `health.go` uses `*gin.Context`.
  - `json.go` removed if redundant.
  - `go build ./...` passes.

### Task 002: Dockerization (T1.3)
- **Type:** Feature
- **Priority:** High
- **What:** Create `Dockerfile` and verify build.
- **Acceptance Criteria:**
  - `api/Dockerfile` exists with correct content.
  - `docker build` succeeds.

## Execution Order
1. Task 001 (Gin Migration)
2. Task 002 (Dockerization)

## Risk Assessment
- **Breaking changes:** Migrating to Gin changes handler signatures. I must ensure all handlers are updated.
- **Dependency issues:** `go mod tidy` should be run to ensure clean dependencies.

## Resource Requirements
- Go 1.23
- Docker

## Execution Plan
- **Phase 1:** Gin Migration
- **Phase 2:** Dockerization
- **Phase 3:** Verification
